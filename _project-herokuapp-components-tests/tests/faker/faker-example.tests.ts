import { test } from "../../fixture-extention";

test.describe.parallel('Faker example tests', async () => {
    test('Verify page content', async () => {
        // Arrange

        //Act
        
        //Assert
    });
});