name: Node.js Build and Playwright Test on PR

on:
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
    - uses: actions/checkout@v2

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install Root Dependencies
      run: npm install

    - name: Run Build
      run: npm run build

    - name: Install Dependencies for Playwright Tests
      run: npx playwright install --with-deps
      working-directory: _project-herokuapp-components-tests

    - name: Run Playwright Tests
      run: npx playwright test
      working-directory: _project-herokuapp-components-tests

    - name: Upload Playwright Test Results
      uses: actions/upload-artifact@v2
      if: always() # ensures that reports are uploaded even if tests fail
      with:
        name: playwright-json-test-results
        path: _project-herokuapp-components-tests/test-results/results.json